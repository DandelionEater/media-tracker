name: Build Linux release artifacts

on:
  push:
    branches:
      - linux-port
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-linux:
    name: Build Linux installers
    runs-on: ubuntu-24.04
    timeout-minutes: 30

    steps:
      - name: Check out selected branch
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: |
            backend/package-lock.json
            frontend/package-lock.json

      - name: Install frontend dependencies
        run: npm ci
        working-directory: frontend

      - name: Install desktop dependencies
        run: npm ci
        working-directory: backend

      - name: Install Flatpak build dependencies
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install --yes flatpak flatpak-builder
          flatpak remote-add --user --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
          flatpak install --user --noninteractive flathub \
            org.freedesktop.Platform//25.08 \
            org.freedesktop.Sdk//25.08 \
            org.electronjs.Electron2.BaseApp//25.08

      - name: Lint frontend
        run: npm run lint
        working-directory: frontend

      - name: Build Linux release artifacts
        run: npm run dist:linux
        working-directory: backend

      - name: Prepare permission-preserving archive and checksums
        id: artifact
        shell: bash
        run: |
          version="$(node -p "require('./backend/package.json').version")"
          artifact_dir="release/${version}/linux"
          archive="${artifact_dir}/Seenary-${version}-linux-unpacked.tar.gz"

          tar -C "${artifact_dir}" -czf "${archive}" linux-unpacked
          (
            cd "${artifact_dir}"
            sha256sum ./*.AppImage ./*.flatpak ./*.tar.gz > SHA256SUMS
          )

          echo "version=${version}" >> "$GITHUB_OUTPUT"
          echo "artifact_dir=${artifact_dir}" >> "$GITHUB_OUTPUT"

      - name: Upload Linux release bundle
        uses: actions/upload-artifact@v6
        with:
          name: seenary-linux-release-${{ steps.artifact.outputs.version }}-${{ github.run_id }}
          path: |
            ${{ steps.artifact.outputs.artifact_dir }}/*.AppImage
            ${{ steps.artifact.outputs.artifact_dir }}/*.flatpak
            ${{ steps.artifact.outputs.artifact_dir }}/*.tar.gz
            ${{ steps.artifact.outputs.artifact_dir }}/SHA256SUMS
          if-no-files-found: error
          retention-days: 14
          compression-level: 0

      - name: Publish Linux assets to GitHub release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_EVENT_TAG: ${{ github.event.release.tag_name }}
        run: |
          version="${{ steps.artifact.outputs.version }}"
          artifact_dir="${{ steps.artifact.outputs.artifact_dir }}"
          tag="${RELEASE_EVENT_TAG:-v${version}}"

          if ! gh release view "${tag}" --repo "${GITHUB_REPOSITORY}" >/dev/null 2>&1; then
            echo "::notice::Release ${tag} does not exist yet; keeping the Linux bundle as a workflow artifact."
            exit 0
          fi

          gh release upload "${tag}" \
            "${artifact_dir}"/*.AppImage \
            "${artifact_dir}"/*.flatpak \
            "${artifact_dir}"/*.tar.gz \
            "${artifact_dir}/SHA256SUMS" \
            --clobber \
            --repo "${GITHUB_REPOSITORY}"
